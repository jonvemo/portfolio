export const SOCIALMEDIA = () =>{
    
}
/*
const 
        SECTIONS = document.querySelectorAll("section"),
        $LIST = document.getElementById('list'),
        $TEMPLATE = document.getElementById('template__contents').content,
        $FRAGMENT = document.createDocumentFragment()
export const CONTENTS_TABLE = 
    SECTIONS.forEach((el,i)=> {
        const 
                SECTION = SECTIONS[i],
                $MODULE = SECTION.querySelector('h3').textContent,
                $MODULE_DESCRIPTION = SECTION.querySelector('h3').getAttribute('data-description') || 'No Description',
                $ID = SECTION.querySelector('h3').id

        i++
        // $TEMPLATE.querySelector('a').dataset.description = $MODULE_DESCRIPTION
        //$TEMPLATE.querySelector('a').href = `#module__${i}`
        $TEMPLATE.querySelector('a').href = `#${$ID}`
        $TEMPLATE.querySelector('a').textContent = `Module ${i}: ${$MODULE}`
        $TEMPLATE.querySelector('dd').textContent = $MODULE_DESCRIPTION
        let $clone = document.importNode($TEMPLATE,true)
        $FRAGMENT.appendChild($clone)
    })
$LIST.appendChild($FRAGMENT)*/